# TODO: remove or replace this query when you replace the artist schema.
query ListArtistAlbums {
  artists {
    nodes {
      name
      albums {
        name
        releasedOn

        tracks {
          name
        }
      }
    }
  }
}
